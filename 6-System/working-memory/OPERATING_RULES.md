# OPERATING_RULES — 系统操作性规则

> 已确认的执行约定与流程规范。自动注入每次 session。
> 更新级别：A 类（Identity 相关内容需走审批）。
> 写入来源：/digest 确认的候选条目从 candidates.md 晋升至此。
>
> ⚠️ 这是「系统工作记忆」，不是「用户记忆主入口」。
> 了解奥一是谁/偏好/行为模式 → 优先读 `5-Identity/` 下各文件。

## 操作性规则

### 写作系统文件命名
- 格式：`YYYYMM-DDHHMM-标题.md`（日期在前，标题在后）
- 生成文件名时用 `date +"%Y%m-%d%H%M"` 拿时间戳，不手动拼
